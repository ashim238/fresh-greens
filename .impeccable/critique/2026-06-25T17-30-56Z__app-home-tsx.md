---
target: app/home.tsx
total_score: 30
p0_count: 0
p1_count: 0
timestamp: 2026-06-25T17-30-56Z
slug: app-home-tsx
round: 7
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Calculating-route + corridor-retry chips strong; neighborhoodLabel can sit on "Your area" indefinitely if reverse-geocode stalls — no spinner on the title row |
| 2 | Match System / Real World | 4 | Chips-as-briefing thesis IA; daylight strip + polyline agree; bookmark/star register matches Maps conventions |
| 3 | User Control and Freedom | 3 | Clear-X + route-cycle chevrons + Recenter/Report FABs; placement-mode Cancel is top-of-sheet thumb reach on large phones |
| 4 | Consistency and Standards | 4 | FAB stack now uses named constants (`FAB_ANCHOR_GAP`, `FAB_HEIGHT`, `FAB_STACK_GAP`); secondaryRow dot separator matches /en-route |
| 5 | Error Prevention | 3 | No-route + retry wired; Go commits without inline "wrong route?" undo — rank carries to /en-route (acceptable but recall-heavy) |
| 6 | Recognition Rather Than Recall | 3 | Hazard chips named with counts; MapCoach is one-shot — long-press-to-save-home unrecoverable except Menu → Map Guide |
| 7 | Flexibility and Efficiency | 3 | Route cycling + Schedule chip; AX5 long chip rows still visually busy |
| 8 | Aesthetic and Minimalist Design | 3 | Hero collapsed (title + ETA + arrival); placement hint is two sentences against otherwise spare card |
| 9 | Help Users Recognize, Diagnose, Recover | 3 | Corridor-retry excellent; tripZonesFetchFailed retry chip-only; suggestedDeparture "Heads up!" doesn't explain daylight why |
| 10 | Help and Documentation | 2 | Map guide coach is sole contextual help; All-clear chip semantics not described for VoiceOver |
| **Total** | | **30/40** | **Good — follow-up sweep closed FAB math + AX5 scroll; help/recovery + coach one-shot remain** |

## Anti-Patterns Verdict

**LLM assessment**: No AI-slop tells. Route-preview card executes brand voice — chips above ETA is thesis IA, reserved-color discipline holds, Calm-Physics on route-cycle slide respects reduceMotion.

**Deterministic scan**: Skipped (React Native source — no HTML detector target). Manual token pass: no raw hex/fontSize in audited regions.

**Visual overlays**: Browser injection skipped (RN source).

## Overall Impression

Round 7 delta vs 2026-06-24 (+1): the follow-up sweep materially helped — FAB constants and `ScrollView` + `flexShrink:1` on route-preview body address two standing P2s. The screen is the calm-companion anchor. Remaining work is consolidation (coach discoverability, ambient loading on neighborhood label) not addition.

## What's Working

- **Chips-above-ETA hierarchy** still reads "is this safe?" before "how long?" — thesis claim in IA form.
- **AX5 scroll inside bottom sheet** keeps Go sticky while content scrolls — the right fix for long route stacks.
- **Named FAB offsets** make the Recenter/Report stack maintainable.

## Priority Issues

- **[P2] MapCoach one-shot buries long-press-to-save-home.** After first dismissal, affordance is Menu → Map Guide only. **Fix**: optional re-show from Map Guide row (exists) + consider session-scoped coach on first placement mode. **Suggested command**: `/impeccable clarify app/home.tsx`
- **[P2] Neighborhood title stalls without feedback.** `neighborhoodLabel` reverse-geocode can leave "Your area" with no in-progress signal. **Fix**: brief "Locating…" on title row or skeleton chip. **Suggested command**: `/impeccable polish app/home.tsx`
- **[P3] Placement-mode hint copy runs long.** "Tap the map to move the pin. Drag to move around." → one line. **Suggested command**: `/impeccable clarify app/home.tsx`

## Persona Red Flags

**Casey (Distracted Mobile)**: Top-row chevrons + Clear-X compete in the sheet header strip — three tappables in a thumb-stretch zone on 6.7" devices.

**Sam (Accessibility)**: All-clear chip and daylight strip lack explicit `accessibilityLabel` semantics beyond visible text — hazard story may not fully transfer to VoiceOver.

## Minor Observations

- `MapCoach` body is three instructions in one card — numbered list register would help (sanctioned in DESIGN.md for scaffolding).
- `suggestedDeparture` footnote is first to overflow at AX5 even with ScrollView — correct shed candidate.

## Questions to Consider

- Could trustedOnRouteRow + suggestedDeparture compose into one contextual sentence?
- Does the chevron-pair belong on the chips strip ("← 1 of 3 →") to declutter the escape row?
