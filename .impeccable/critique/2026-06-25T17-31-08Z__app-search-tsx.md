---
target: app/search.tsx
total_score: 31
p0_count: 0
p1_count: 1
timestamp: 2026-06-25T17-31-08Z
slug: app-search-tsx
round: 7
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Loading/error differentiated; 300ms debounce window has no mid-flight feedback |
| 2 | Match System / Real World | 4 | "Where are you headed?", `relativeWhen`, recents register all on-brand |
| 3 | User Control and Freedom | 3 | Back chevron persistent; clear wipes query + tool selection; long-press remove on recents undiscoverable |
| 4 | Consistency and Standards | 3 | `quickTool.borderRadius: 8`, `gap: 16` still raw literals vs `theme/radii` + `theme/spacing` |
| 5 | Error Prevention | 3 | Silent autocomplete + `lastQueryRef` stale-guard correct; explicit-submit path when location null |
| 6 | Recognition Rather Than Recall | 4 | Recents, Saved, Upcoming, Quick Tools surface memory aids |
| 7 | Flexibility and Efficiency | 3 | Gas category wired; no re-trigger if results stale without clear + re-select |
| 8 | Aesthetic and Minimalist Design | 3 | Landing appropriately sparse; demo fuel footnote orphaned without enclosing surface |
| 9 | Error Recovery | 3 | No-results icon swap thoughtful; "Locating you…" diagnoses without remedy |
| 10 | Help and Documentation | 2 | Inert mic glyph at full opacity looks tappable to sighted users — documented intentional deferral |
| **Total** | | **31/40** | **Good — unchanged score; mic affordance + token drift are standing gaps** |

## Anti-Patterns Verdict

**LLM assessment**: Not AI slop. State-machine comment block reads engineering-heavy but choices are rationale-backed (silent autocomplete, dedup, relativeWhen).

**Deterministic scan**: Skipped (RN source).

**Visual overlays**: Browser injection skipped (RN source).

## Overall Impression

Unchanged from closeout — screen wasn't edited in the tow-pick arc. The mic icon decision is now explicitly documented in-source (omit `onMicPress` to avoid false VoiceOver "button" — sighted-user inertness remains the load-bearing P1).

## What's Working

- **Quick Tools two-path model** (query mirror vs Saved inline list) is honest IA.
- **Clear deselects tool filter** — prevents "tile selected but query empty" confusion.
- **Recents long-press remove** works when discovered — copy could surface the gesture.

## Priority Issues

- **[P1] Mic icon looks tappable but is inert (sighted users).** `PressableIcon` renders plain `View` when `onMicPress` undefined — correct for VoiceOver, wrong for sighted affordance. **Fix**: reduce opacity + `accessibilityElementsHidden` on decorative mic, or ship voice stub. **Suggested command**: `/impeccable quieter app/search.tsx` + `components/SearchBar.tsx`
- **[P2] Raw spacing/radii literals in Quick Tools row.** `borderRadius: 8`, `gap: 16` should use `radii.sm` / `spacing.md`. **Suggested command**: `/impeccable polish app/search.tsx`
- **[P2] Silent debounce — no typing feedback.** **Fix**: subtle trailing ActivityIndicator in search bar during fetch. **Suggested command**: `/impeccable animate app/search.tsx`
- **[P3] Demo fuel price footnote visually orphaned.** **Suggested command**: `/impeccable layout app/search.tsx`

## Persona Red Flags

**Jordan (First-Timer)**: Long-press to remove recents has no discoverability cue — will never find it.

**Alex (Power User)**: Can't refresh stale autocomplete without clear-and-retype.

## Minor Observations

- Inline comment at line 630 documents mic decision well — keep when voice ships.
- PR #235 `SafetyErrorMessage` pattern didn't propagate here — error copy style differs from safety flows.

## Questions to Consider

- Should the mic be removed entirely until voice ships, vs muted decorative glyph?
- Could recents rows show a subtle "hold to remove" footnote on first visit?
