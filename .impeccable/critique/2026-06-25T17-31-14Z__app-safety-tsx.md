---
target: app/safety.tsx
total_score: 33
p0_count: 0
p1_count: 2
timestamp: 2026-06-25T17-31-14Z
slug: app-safety-tsx
round: 7
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Active share session invisible on picker; cross-tile conflict surfaces only via Alert after wrong tap |
| 2 | Match System / Real World | 4 | Toolkit tile labels + illustration distinctiveness; SOS lane separated below situational grid |
| 3 | User Control and Freedom | 3 | System swipe-dismiss works; no painted close-X per PR #241 convention on other modals |
| 4 | Consistency and Standards | 3 | SOS row has `accessibilityHint`; four toolkit tiles set `accessibilityLabel` only — drift from PR #242 label+hint rule applied to sub-flows |
| 5 | Error Prevention | 3 | No-contact gate reactive (Alert after tap on Share location); cross-tile guard same pattern |
| 6 | Recognition Rather Than Recall | 3 | "Unfamiliar area" vs "Share location" semantically close; no subtitle disambiguation |
| 7 | Flexibility and Efficiency | 4 | Four-tile grid scannable; no deep-link to sub-flow |
| 8 | Aesthetic and Minimalist Design | 4 | `marginTop: 'auto'` SOS anchor elegant; navy shield matches /en-route FAB |
| 9 | Error Recovery | 3 | Alert dialogs informational — no inline tile state for blocked paths |
| 10 | Help and Documentation | 3 | "What's going on?" held-question register correct; tiles lack outcome hints for first-timers |
| **Total** | | **33/40** | **Good — held score; sub-flows outpolished the picker door they route through** |

## Anti-Patterns Verdict

**LLM assessment**: No AI-slop tells. Four-tile grid saved by per-tile illustration semantics; Title1 Regular held-question + SOS hairline separation are intentional brand choices.

**Deterministic scan**: Skipped (RN source).

**Visual overlays**: Browser injection skipped (RN source).

## Overall Impression

The safety modal is now the **polish boundary** of the safety arc: `/roadside` gained tow-pick + status card fixes in PR #253; `/pulled-over`, `/share-location`, `/unfamiliar` picked up hints and inline errors — but `/safety` itself wasn't touched. Score holds; inconsistency with sub-flow conventions is sharper.

## What's Working

- **SOS lane separation** — red burst icon confined to Emergency row, not ambient tint.
- **Illustration distinctiveness** per tile prevents generic card-grid slop.
- **Cross-tile guard logic** prevents conflicting share sessions (discoverability is the gap, not the rule).

## Priority Issues

- **[P1] Toolkit tiles missing `accessibilityHint` (PR #242 convention).** Sub-flow pickers ship `label + present-tense outcome hint`; safety modal tiles stop at noun labels. **Fix**: add hints like "Opens pulled-over recording and guidance". **Suggested command**: `/impeccable harden app/safety.tsx`
- **[P1] Active share session not visible on picker.** User must tap wrong tile to learn they're blocked. **Fix**: ambient banner or dim incompatible tiles when `session` active. **Suggested command**: `/impeccable clarify app/safety.tsx`
- **[P2] No painted close-X.** PR #241 standardized X on content sheets; safety modal relies on swipe-only + DragHandle decorative. **Suggested command**: `/impeccable polish app/safety.tsx`
- **[P2] Unfamiliar vs Share location disambiguation.** Single-word tile labels don't explain routing vs live-share difference. **Suggested command**: `/impeccable clarify app/safety.tsx`
- **[P3] Raw spacing literals in header/SOSBar** — `gap: 16`, `gap: 8` vs tokens. **Suggested command**: `/impeccable polish app/safety.tsx`

## Persona Red Flags

**Jordan (First-Timer)**: Can't distinguish Unfamiliar area from Share location without tapping — both sound like "I'm lost."

**Sam (Accessibility)**: Tiles lack outcome hints; SOS row is the only tile following house style.

**Black driver in charged moment**: Four-tile picker is one extra tap before the relevant sub-flow; Pulled-over top-left is correct for LTR urgency.

## Minor Observations

- `accessibilityHint` on Emergency row (line 239) — closeout F3 "SOS lacks hint" is stale.
- Sub-flow polish boundary is the story of Round 7 for this screen.

## Questions to Consider

- Should active session state dim the incompatible tile instead of Alert-on-tap?
- Does the picker need a one-line subtitle under each tile label?
